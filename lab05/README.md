Lab 5: Cos-Distance

How to compile and run the unit tests:

Open a terminal in the lab05 directory.

Compile using:

g++ main.cpp cosine.cpp

Run:

./a.out vectors.txt

where vectors.txt contains the input vectors.

Compile and run unit tests using:

g++ cosine.cpp test.cpp

If all tests pass, doctest will print SUCCESS.