Lab 1: Prefix Sums

How to compile and run the unit tests:

1. Open a terminal in the lab01 directory.
2. Compile using:

   g++ prefix_sums.cpp prefix_sums_tests.cpp

3. Run:

   ./a.out

If all tests pass, doctest will print SUCCESS.